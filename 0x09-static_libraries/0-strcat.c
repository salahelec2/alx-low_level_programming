#include "main.h"
#include <string.h>

/**
 * _strcat - Conacatenates two strings together
 * @dest: the host string
 * @src: this string will be apended to the host string
 * Return: concatenated string
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
