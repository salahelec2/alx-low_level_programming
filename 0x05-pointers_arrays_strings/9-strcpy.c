#include "main.h"

/**
 * puts2 - print half
 **@str: string
 *
 * Return: void
 */

char *_strcpy(char *dest, char *src)
{
	int i;
	for (i = 0; src[i] != '\0'; i++)
	{
		dest[i] = scr[i];
	}
	dest[i] = src[i];
	return (dest);
}
